<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="20dp"
    android:background="@color/color_base_background"
    android:orientation="vertical" >

    <ExpandableListView
        android:id="@+id/skill_list"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:divider="@color/color_base_title"
        android:dividerHeight="1px"
        android:groupIndicator="@android:color/transparent"
        android:scrollbars="none" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="@color/color_base_body" >

        <TextView
            android:id="@+id/skill_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal"
        android:padding="10dp" >

        <Button
            android:id="@+id/skill_dialog_cancel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginRight="20dp"
            android:layout_weight="1"
            android:background="@drawable/selector_button"
            android:paddingBottom="5dp"
            android:paddingTop="5dp"
            android:text="@string/cancel"
            android:textColor="@color/color_base_title"
            android:textSize="14sp" />

        <Button
            android:id="@+id/skill_dialog_ok"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="20dp"
            android:layout_weight="1"
            android:background="@drawable/selector_button"
            android:paddingBottom="5dp"
            android:paddingTop="5dp"
            android:text="@string/ok"
            android:textColor="@color/color_base_title"
            android:textSize="14sp" />
    </LinearLayout>

</LinearLayout>